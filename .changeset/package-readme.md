---
"cyber-mux": patch
---

Ship a readme with the package — the repository readme now lives in `packages/cyber-mux/` (symlinked
from the repo root), so npm shows the badges, backend list, and command table instead of nothing.
