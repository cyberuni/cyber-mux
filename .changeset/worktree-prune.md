---
"cyber-mux": minor
---

Add `worktree prune` to remove every disposable worktree in one call — the same gate `worktree list` marks `(removable)` with. The bare form previews the candidates; pass `--force` to actually remove them.
