---
status: implemented
project-path: packages/cyber-mux
approval:
  spec:
    verdict: approve
    by: agent
    cause: dimension
    why:
      floor: Clearance — FIRED and pre-authorized by the requester at intake, the one floor this CR trips. Three scenarios are REMOVED from the frozen layout.feature and re-stated at mux/, which is a narrowing however the net reads: the project's total contract is unchanged (every assertion lands in mux.feature), but the floor keys on the edit class, not the net. Verified structurally rather than by line diff — gherkin-cli diff --base origin/main layout.feature: 0 added / 0 modified / 3 removed, exactly the scenarios declared, nothing else touched. The lifecycle's pure-move/rename exemption (zero content delta preserves freeze) is deliberately NOT claimed: the moved scenarios were reworded from template voice to seam voice, so there is no zero-delta git mv to point to, and the cold judge independently confirmed that a producer invoking that exemption here would have been wrong. The mux/ side is the opposite class — 11 added / 0 modified / 0 removed, addOnly true, so its freeze self-clears with no re-open. Compatibility: no behavior change at all (spec + one doc comment), so no semver class to exceed.
      blast: medium — two behavioral nodes, one owning move rather than an addition. mux/ gains the seam's split-options contract (11 scenarios, a README use case, a behavior-table row); layout/ loses the three seam-convention scenarios and is repointed. The trigger is issue #10, which the previous CR's own ledger names as the debt it was deferring, so this pays a tracked debt rather than reversing a live decision. Scope held to the three fields the issue names: describeRegion is the other seam member specified through layout under the same house style and is deliberately left out. No production code changes.
      novelty: high — the issue's premise checked out, but the ground under it was worse than filed. mux.feature HAS NEVER PARSED: an Examples table declares | branch | placement | while two of its three rows carry one cell, so Gherkin rejected the whole file and the entire frozen suite was invisible to check-suite and bound to no test — the node reported 0 scenarios where it holds 61. That made the repair a blocking prerequisite rather than scope creep, since the spec gate runs check-suite fail-closed over touched suites; repaired in its own commit, taking the node to 61/34 bound/34 pass/0 fail. A second finding contradicts the code's own documentation: session.ts said env is "only meaningful for a pane:* placement" while both backends emit it at EVERY tier and the tests pin exactly that — load-bearing, because a pane pool's root pane is born by the region open and never by a split, so the stale comment instructed precisely the bug that would drop it. Corrected here. Also corrected against the issue: its "tested only through the layout suite" is loose — the fields ARE driven directly at the pane layer, but those tests implement layout.feature's scenarios, so the specification claim holds and the testing claim does not.
      confidence: high — cold sdd-spec-judge (fresh context, independent) 3-lens {oracle,builder,architect} all PASS, ALIGNED true, zero failing scenarios, no blocker, no open markers; mechanical check-suite + check-spec-state green. It re-derived every load-bearing claim rather than trusting the brief: it re-ran both edit classifications structurally; it verified every backend claim against the adapter source rather than this CR's prose (herdr's pane id is positional and no --from flag exists, toTmuxSize(0.333) really is 67%, the !window guards, env at every tier on both backends, herdr's worktree verbs accepting env in their type and never referencing it, canSizeSplits true on both); it ran the miss test over all 11 new scenarios and found none inert; it ran the absorption check in the direction that threatened this CR (scenarios authored after reading the implementation) and found no apparatus in any Given; and it checked pairwise consistency across all 72 scenarios. Two catches worth recording: it reproduced the EPARSE itself by stashing the tree and diffing against the committed baseline, rather than taking the blocker on trust; and it checked the carve-out this CR could have abused, confirming the pure-move exemption does not apply and is not claimed. Three non-blocking observations carried to deliver. Self-asserted within the auto-spec leash (by user) — ratify or kick back.
      cr: 10-split-options-contract
  impl:
    verdict: approve
    by: agent
    cause: dimension
    why:
      floor: none at the impl gate — the CR's one floor (Clearance, for the layout narrowing) fired and was pre-authorized at the spec gate, and nothing at deliver widened it: the layout edit is still exactly the 3 declared scenarios. No frozen scenario was narrowed, rewritten or deleted here, and no scenario was added at this gate. Every defect the judges found was fixed in code, in tests, or in a commit that was missing — never by editing the contract to fit the implementation. Compatibility: no behavior change at all, so there is no semver class to exceed; the only production edit across five commits is a doc comment, and round 2 independently confirmed no behavior was smuggled into it.
      blast: medium — two behavioral nodes and no production code. The delivery is a binding job plus one new test: the adapter tests already drove all three fields but bound to nothing, because the bridge keys on a test's leaf title being the scenario name verbatim. The move RETIRED debt rather than shifting it: the three scenarios that left layout were themselves unbound there, so those facts are now verified at mux rather than merely stated at layout. RE-GATED after the optimistic rebase lost its race — CR 9 landed mid-PR and touched every file this CR touches (both suites, both READMEs, spec.md, session.ts, both adapters, both adapter test files), so the branch was re-rebased and this gate re-run on the merged tree before any push. Only spec.md conflicted, twice, and both were the same current-state-twin collision resolved in this CR's favour with CR 9's verdicts durably intact in their own shard. This CR's own table-repair commit was dropped by the rebase as byte-identical to CR 9's independent repair of the same bug — verified a true no-op, not a silent loss. Bridge on the merged tree: 48/77 bound, 48 pass, 0 fail, 29 unbound — all 11 of this CR's scenarios bind and pass, and the 29 unbound are the untouched pre-existing baseline, so no unbound debt is added.
      novelty: high — the delivery's two hardest facts were both invisible to reading. The `!window &&` guards on tmux's `from` and `size` are DEAD DEFENSE: the window branch spreads neither value, so removing either guard alone leaves the whole suite green. This was first believed to be the gap the new test closes; mutation proved otherwise, and both scenarios turn out loseable only by the compound wrong subject (guard removed AND the value wired into the window branch). The guards are kept — they cost nothing and state the intent locally, and deleting them is a production change a spec-only CR has no business making. Second: mux.feature had never parsed, so the node's entire frozen suite was invisible and bound to nothing (0 scenarios where it holds 61) — repaired as a blocking prerequisite, since the spec gate runs check-suite fail-closed over touched suites.
      confidence: high — TWO cold sdd-impl-judge rounds, each a fresh context re-deriving every scenario's oracle from the adapter source rather than from this CR's prose. R1 returned CHANGE on two real defects, both found by running rather than reading, and the first would have shipped: the session.ts doc-comment correction was NEVER COMMITTED — the spec commit's message claimed it, but the staging line added only the spec directory, so the file stayed uncommitted drift across all three commits and HEAD's interface doc still contradicted the scenario this CR had just frozen. No test could catch it (the change is a comment) and the message asserted it was done; the judge caught it only by running git show --name-only per commit instead of trusting the message. R1's second: the herdr test bound to "env with no launch" ruled out `pane run` but not send-text/send-keys — a rename-induced over-claim where the retitled test promised more than its body checked, proven by mutation (a stray bare-Enter submit left all 73 herdr tests green). Both fixed; the mutation now fails. R2 re-ran all three mutations BY HAND rather than confirming from the report, and went past R1 to check every renamed test for the same over-claim shape (none found), that all 6 Examples rows of the env-tier scenario are individually bound, and that scenario 10 verifies createInWorkspace AND openInWorkspace separately. IMPLEMENTATION_PASS true, 11/11 scenarios pass, no blocker, no absorption findings. One R2 observation was acted on rather than deferred: a pre-existing blank line severed the mux README's behavior table so this CR's own row rendered under no header, and the stale duplicate placement row below it was a strict subset of the row above — both removed. A THIRD cold round then re-judged the post-rebase merged tree from scratch, re-deriving all 11 oracles against CR 9's rewritten adapters rather than re-reading the earlier rounds, and attacked the merge on every axis: it confirmed CR 9's OpenedPane widening leaves these argv-shaping scenarios untouched (production source byte-identical to origin/main but for this CR's doc comment), that the merged 77 scenarios carry no contradiction or overlap, that the spec.md conflict resolution destroys nothing durable, that layout.feature's delta against the NEW origin/main is exactly the 3 declared removals with none of CR 9's edits clobbered, and — by direct mutation on the current source rather than on trust — that both !window guards remain dead defense after the rewrite. Six mutation trials, every one caught. IMPLEMENTATION_PASS true, 11/11, no blocker, no observations. pnpm verify green, 431 tests. Self-asserted within the auto-spec leash (by user) — ratify or kick back.
      cr: 10-split-options-contract
---

# cyber-mux — the CLI: cross-multiplexer pane control

> Root project spec — the **descriptive** top index for the `cyber-mux` npm package
> (`packages/cyber-mux`). Behaviors live in the capability folders below.

`cyber-mux`: one contract (`SessionAdapter`) over terminal multiplexers (tmux, herdr) — detection,
pane identity, placement, git worktree, and turn-taking (nudge) helpers — decoupled from legion
(no store/identity/doorbell). Env namespace is `CYBER_MUX` / `CYBER_MUX_PANE`.

## Capabilities

| Node | Concern |
|---|---|
| [`mux/`](./mux/README.md) | the pane abstraction — backend selection, placement, multiplexer detection, focus reporting |
| [`layout/`](./layout/README.md) | named, reusable pane layouts — template resolution, the schema, and the walk that builds a pool against a target cwd |
| [`axi/`](./axi/README.md) | the Agent Experience Interface output contract every CLI command follows |
| [`design/`](./design/README.md) | cross-cutting rules/models and the decisions log (append-only, descriptive, ungated) |
