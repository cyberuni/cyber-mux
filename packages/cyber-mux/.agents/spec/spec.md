---
status: implemented
project-path: packages/cyber-mux
approval:
  spec:
    verdict: approve
    by: unional
    cause: ratified
    why:
      floor: "Clearance FIRES and is RATIFIED in-session, twice. This CR narrows and rewrites frozen scenarios rather than only adding — structural diff addOnly FALSE on both suites (mux.feature: 11 added, 5 modified, 1 renamed; layout.feature: 4 modified). The first re-open, ratified up front, covers the six mux.feature scenarios the stream flip touches — bare-send and the five ambiguity-report scenarios, two of which encoded stderr-keeps-stdout-clean as their own rationale and so had their argument rewritten, not just their stream swapped. The second re-open, ratified after the spec-judge's round 2 found it, extends Clearance to layout.feature: leaving it frozen was not a gap but a Conflict, since one validator (isValidLayoutName) was contracted at exit 1 there and exit 2 by this CR's new layout-save row, which no implementation could satisfy. Compatibility does NOT fire and was checked, not assumed: package is 0.0.0, nothing shipped, so a breaking change to the exit-code and stream contract exceeds no ceiling. No Conflict remains. Changeset owed at handoff."
      blast: "medium — one REFERENCE contract node shared beyond this repo (axi/), plus two behavioral suites and their nodes. The axi/ node is rewritten to match upstream AXI on every point it had drifted: errors move to stdout (#6), usage errors to exit 2 (#6/#8), errors gain a stable code and a help line (#6), and suggestions move to stdout and gain the omit-when-self-contained rule (#9). Two unargued widenings the spec-judge caught mid-CR were retracted — #8 restated AXI's bare-BINARY rule as covering any command group, and #10 had dropped its tool-identity half — both the same local-restatement defect this CR exists to repair, one section over. exists's 1=gone is recorded as a genuine divergence rather than the 'amendment' the corpus had mislabeled it. The real radius is that axi/ names cyberplace and universal-plugin as adopters of the SAME contract, so following AXI on streams here diverges from them until they follow; that is recorded as a follow-up, not fixed from a node this bin does not own. No production code changes; the fail() sweep across all ~15 verbs lands at deliver."
      novelty: "high — this CR's own lineage is the finding. It is the third mission to touch AXI's exit-code paragraph (after #31 invented a meaning for a code it thought absent, and #37 corrected that), and the defect each time was trusting this repo's restatement over the upstream spec. Reading upstream directly surfaced a fourth divergence the issue never named (#9 suggestions on the wrong stream, demanded of every command) and two the issue could not have named (#8's widening, #10's dropped half) — all invisible to anyone who read only the local paragraph. The load-bearing subtlety the reclassification forced out: a validate reporting a template's content invalid is a predicate answer like exists, and a mutating verb refusing a bad template is an operation failure — neither is the malformed-argument family AXI puts at 2, so only four of layout's twenty-two exit-1 pins are genuine usage errors. Drawing that line wrong in either direction would have either missed a usage error or mislabeled a failure."
      confidence: "high — three cold spec-judge rounds, each a fresh context re-deriving rather than confirming, and the first two FAILED all three lenses. R1 caught the CR committing the exact defect it exists to repair: the upstream-read method was applied to the two principles under repair and not to the two beside them, so #8 was found widened and #10 half-dropped; it also caught the consumer node contradicting the suite in three places, one of them a line this CR had edited and left half-corrected, and a fourth contradiction in an append-only ADR never swept into the touch set. R2 caught the false-tense class surviving by changing tense — 'is built' (a prior CR's bug) had become 'is filed' four times over nothing filed, one baked into a frozen comment — and, decisively, found the layout Conflict that a prose fix could not reach, forcing the second ratified re-open. R3 passed all three lenses: it independently re-derived all twenty-two layout exit-code pins and agreed with the split, re-read upstream AXI on every principle #1-#10 and found each faithfully restated, verified the #9 partly-built-on-stderr claim against cli.ts:244 and :499, swept the corpus for new conflicts introduced by the reclassification and found none, and confirmed the false-tense class dead against source. Self-asserted within the auto-spec leash, then RATIFIED by the requester in-session — so this gate carries a human verdict, not an agent's standing claim, and the two hard-floor re-opens it rests on were both ratified live."
      cr: "36-axi-error-surface"
  impl:
    verdict: approve
    by: agent
    cause: dimension
    why:
      floor: "none at the impl gate. Every frozen scenario was built against, not fitted to: no .feature was edited at deliver, no frozen scenario narrowed or added. The suite's two re-opens both happened at the spec gate under ratified Clearance; the impl gate froze that contract and built to it. The one place a frozen scenario governs a stream/exit — the ambiguity report, exists's three codes, the four layout usage errors — the implementation was driven on the real binary and matches. Compatibility does not fire: package is 0.0.0, nothing shipped, so moving errors to stdout and usage errors to exit 2 breaks no existing caller's contract."
      blast: "medium — the CLI's entire error surface, no adapter or seam change. 22 fail() sites collapsed to one CliError model, one stdout renderer (reportError), one guarded() boundary, a paneVerb() wrapper that translates a backend not-found into pane-not-found rather than forwarding the multiplexer's raw text, and a handleCommanderError that routes the parser's own usage errors (unknown flag, missing argument, mutually exclusive flags, bare group) through the same coded surface at exit 2. New file src/cli-error.ts; changes confined to src/cli.ts + src/cli.test.ts (+ a one-line stale-comment fix in output.ts that this CR made false). The seam is provably untouched — session.ts, both adapters, backend.ts and worktree.ts are byte-identical to HEAD, confirmed by the impl-judge. One accepted cosmetic cost: for parser-level usage errors commander still writes its own terse text to stderr before the structured error emits on stdout; no frozen scenario forbids it, and the scenarios that require stderr-empty (the verb-failure and ambiguity paths, which route through reportError not commander) have it empty, driven and asserted."
      novelty: "medium — the surface was well-specified by the frozen suite, so deliver was execution rather than discovery, but the code taxonomy carried real judgment the suite constrained without dictating: which of the 22 sites are usage errors (2) versus operation failures (1), and the translate-don't-forward rule for backend text. The one subtlety that could have gone wrong quietly: the partial layout apply is NOT a structured error — it reports the manifest of what it built as the whole of stdout and exits nonzero, the failed pane named inside that one payload — so it had to be kept OUT of the CliError path, which the impl-judge verified by JSON-parsing the whole stdout and finding one manifest, not a manifest plus an error object."
      confidence: "high — cold sdd-impl-judge, fresh context, re-deriving each frozen scenario's oracle from cli.ts/cli-error.ts rather than from the producer's report, and it refused to trust that report. It re-ran pnpm verify itself (7/7 turbo tasks, 559 tests, biome ci clean, both features parse), drove the REAL binary for the load-bearing cases (no-mux on stdout/exit1/stderr-empty, bare send help on stdout/exit2, layout list --force → unknown-flag/2 listing only list's flags, read no-arg → missing-argument/2, --help → 0, ambiguous → stdout/2), read every bound test's BODY rather than its title for the retitled-over-claims shape and found none, confirmed the 19 layout exit-1 pins did not regress, and verified the flipped tests were re-pointed stderr→stdout with exit 1→2 while keeping their specific assertions rather than weakened to toContain. Absorption clean: no scenario literal special-cased in production. IMPLEMENTATION_PASS true, no blocker. Its two observations were acted on (the stale output.ts comment) or are the conductor's to close (spec authorship, confirmed from this ledger). Self-asserted within the auto-spec leash; the spec gate of this CR was ratified in-session by the requester."
      cr: "36-axi-error-surface"
---

# cyber-mux — the CLI: cross-multiplexer pane control

> Root project spec — the **descriptive** top index for the `cyber-mux` npm package
> (`packages/cyber-mux`). Behaviors live in the capability folders below.

`cyber-mux`: one contract (`SessionAdapter`) over terminal multiplexers (tmux, herdr) — detection,
pane identity, placement, git worktree, and turn-taking (nudge) helpers — decoupled from legion
(no store/identity/doorbell). Env namespace is `CYBER_MUX` / `CYBER_MUX_PANE`.

## Capabilities

| Node | Concern |
|---|---|
| [`mux/`](./mux/README.md) | the pane abstraction — backend selection, placement, multiplexer detection, focus reporting |
| [`layout/`](./layout/README.md) | named, reusable pane layouts — template resolution, the schema, and the walk that builds a pool against a target cwd |
| [`axi/`](./axi/README.md) | the Agent Experience Interface output contract every CLI command follows |
| [`design/`](./design/README.md) | cross-cutting rules/models and the decisions log (append-only, descriptive, ungated) |
